#!/bin/sh

[ -n "$1" ] && dir="$1" && shift || dir="."
[ -n "$1" ] && names="-name $1" && shift || names=

for name; do names="$names -o -name $name"; done
find $dir -0 -type f "$names" 2>/dev/null | xargs -0 -d'\n' du -s 2>/dev/null | sort -n
